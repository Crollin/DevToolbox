# Override local : build les images au lieu de tirer GHCR
# Usage : docker compose -f docker-compose.yml -f docker-compose.build.yml up --build

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile

  frontend:
    build:
      context: .
      dockerfile: Dockerfile
